# CLI

Use the CLI to add components and more

Options
Use
to scaffold a new Expo project using a template.
yarn@berry
groupId
dark:text-foreground *:dark
npx @react-native-reusables/cli@latest init
bunx --bun @react-native-reusables/cli@latest init
pnpm dlx @react-native-reusables/cli@latest init
yarn dlx @react-native-reusables/cli@latest init
tabIndex
Initialize
Expo
React
Native
Reusables
-c,
directory.
-t,
use.
-h,
Available templates
to add React Native Reusables components to your project.
Add
-y,
-o,
-a,
-p
w-0.5
Uses the
CLI under the hood.
to check if everything is set up correctly.
npx @react-native-reusables/cli@latest doctor
bunx --bun @react-native-reusables/cli@latest doctor
pnpm dlx @react-native-reusables/cli@latest doctor
yarn dlx @react-native-reusables/cli@latest doctor
Check
-s,
Debugging
If you're having issues, you can run any command with the initial
flag to get more information and create a
bug report
Changelog
Built by
Founded Labs
, bringing
shadcn/ui
to React Native. Source on
GitHub
Want to work with us?
Mention us to your team.
Learn more
Learn more about Founded Labs